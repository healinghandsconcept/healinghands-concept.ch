---
translationKey: contact
layout: base.njk
title: Contact - Healing Hands Concept
description: Contact Fabio Gomes for your well-being and prevention needs.
---

<section class="page-header container">
  <h1>Contact</h1>
  <p class="subtitle">A question? A well-being project in Fribourg? Contact us.</p>
</section>

<section class="contact-section container section-padding">
  <div class="contact-grid">
    <div class="contact-info">
      <h2>Our Details</h2>
      <p>Find us at our massage clinic in Fribourg (Route de Beaumont) or request a health intervention directly at your company's premises.</p>
      
      <div class="info-item">
        <strong>Address</strong>
        <p>
          <a href="https://www.google.com/maps/place/The+Healing+Hands+Concept+Fribourg+-+Massage+Th%C3%A9rapeutique,+Duo,+Sportif+et+%C3%A0+domicile/@46.8016421,7.1511211,15z/data=!4m6!3m5!1s0x478e697d264f33fd:0xf79ed1a141b2a953!8m2!3d46.8016421!4d7.1511211!16s%2Fg%2F11sbj9tt1q" target="_blank" rel="noopener noreferrer">
            Route de Beaumont 9c<br>1700 Fribourg (Switzerland)
          </a>
        </p>
      </div>
      
      <div class="info-item">
        <strong>Email</strong>
        <p><a href="mailto:contact@healinghands-concept.ch">contact@healinghands-concept.ch</a></p>
      </div>

      <div class="info-item">
        <strong>Opening Hours</strong>
        <p>Monday - Friday: 08:00 - 19:00<br>Saturday: On request</p>
      </div>
    </div>

    <div class="contact-form-container">
      <form id="contact-form" class="contact-form">
        <div class="form-group">
          <label for="name">Name / Company</label>
          <input type="text" id="name" name="name" required placeholder="Your name or company name">
        </div>
        
        <div class="form-group">
          <label for="email">Email</label>
          <input type="email" id="email" name="email" required placeholder="your@email.com">
        </div>

        <div class="form-group">
          <label for="subject">Subject</label>
          <select id="subject" name="subject">
            <option value="Healing Pass Company">Healing Pass Company</option>
            <option value="Healing Pass Individual">Healing Pass Individual</option>
            <option value="Information Request">Information Request</option>
            <option value="Other">Other</option>
          </select>
        </div>

        <div class="form-group">
          <label for="message">Message</label>
          <textarea id="message" name="message" rows="5" required placeholder="How can we help you?"></textarea>
        </div>

        <button type="submit" class="btn">Send my message</button>
        <div id="form-status" class="form-status"></div>
      </form>
    </div>
  </div>
</section>

<script>
// Logic for Resend Integration (Client-side implementation with instructions)
document.getElementById('contact-form').addEventListener('submit', async function(e) {
  e.preventDefault();
  const statusEl = document.getElementById('form-status');
  const submitBtn = e.target.querySelector('button');
  
  statusEl.textContent = 'Sending...';
  submitBtn.disabled = true;

  // IMPORTANT: Since this is a static site, you need a backend endpoint to keep your Resend API Key secret.
  // We recommend using a serverless function (Vercel, Netlify, or a simple Node interface).
  
  const formData = new FormData(this);
  const data = Object.fromEntries(formData.entries());

  try {
    const response = await fetch('/api/send', { 
      method: 'POST',
      body: formData // Sending FormData directly is often easier, but the function expects JSON? 
                     // Wait, my function code expects FormData? 
                     // Let's check functions/api/send.js
                     // Yes: const formData = await request.formData();
    });

    const result = await response.json();

    if (response.ok) {
      statusEl.textContent = 'Thank you! Your message has been sent successfully.';
      statusEl.className = 'form-status success';
      this.reset();
    } else {
      throw new Error(result.error || 'Error sending message');
    }

  } catch (error) {
    statusEl.textContent = "An error occurred: " + error.message;
    statusEl.className = 'form-status error';
  } finally {
    submitBtn.disabled = false;
  }
});
</script>
